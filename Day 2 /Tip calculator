#If the bill was $150.00, split between 5 people, with 12% tip. 

#Each person should pay (150.00 / 5) * 1.12 = 33.6
#Format the result to 2 decimal places = 33.60

#Tip: There are 2 ways to round a number. You might have to do some Googling to solve this.💪

#Write your code below this line 👇

Total_bill = int(input("Your total bill is: "))

Tip = int(input("How much tip would you like to pay 10, 12 or 15?"))

total_people = int(input("total people you want to split the bill with: "))

tip_percentage = Tip / 100

tip_calculator = tip_percentage * Total_bill

Total_bill = Total_bill + tip_calculator

bill_each = Total_bill / total_people

print(f'each person need to pay: {bill_each}')
